{
  "name": "wath",
  "private": true,
  "license": "Apache-2.0",
  "description": "Onboarding & conformance engine for platform service integrations",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "demo:prewarm": "./scripts/demo-prewarm.sh",
    "demo:run": "./scripts/demo-run.sh",
    "demo:launch": "./scripts/demo-live-launch.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
