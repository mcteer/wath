---
description: "Vault dynamic-secrets conventions; load when editing Vault policy, params, or secret-delivery manifests."
globs: ["**/policy.hcl", "**/integration.params.json", "**/*vault*", "k8s/**/*.yaml", "k8s/**/*.yml"]
alwaysApply: false
---

# Vault dynamic-secrets — working rules

You are editing artifacts governed by `@standards/security/vault-dynamic-secrets/SKILL.md`. Follow it; the
checks below are enforced by `@standards/security/vault-dynamic-secrets/conformance/test_conformance.py`.

- **VDS-001** Consume `database/creds/<role>`. NEVER a KV mount; NEVER a static DSN, username, or
  password in code, env, ConfigMaps, or `Secret` manifests.
- **VDS-002** Database role `default_ttl` ≤ 1800s and `max_ttl` ≤ 3600s.
- **VDS-003** The policy grant for the creds path has capabilities **exactly** `["read"]` — never
  `create`/`update`/`delete`/`list`/`sudo`.
- **VDS-004** NO wildcard paths in policy (`*`, `database/*`, `database/creds/*`). Name the specific
  role path only; Vault is deny-by-default — do not widen it.
- **VDS-005** Auth method matches runtime: kubernetes→`kubernetes`, nomad→`jwt`, vm→`approle`/cloud.
- **VDS-006** Auth role binds a specific identity (no `*` in service accounts/namespaces/claims) and
  attaches the scoped policy — never `default` or a shared/admin policy.
- **VDS-007** Secret delivery via VSO (`VaultDynamicSecret`) or Agent Injector annotations only.
- **VDS-008** Ship a CI workflow that re-verifies the dynamic-secret flow.

<example type="valid">
path "database/creds/orders-api" { capabilities = ["read"] }
</example>
<example type="invalid">
path "database/*" { capabilities = ["read", "list"] }   # wildcard + extra capability → VDS-003/004 fail
</example>

Generate `integration.params.json` first and render policy/role from it. Least-privilege SQL grants
are your best effort — state the assumption in the PR for SME ratification (SKILL.md §6).
